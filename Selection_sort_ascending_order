#include<bits/stdc++.h>
using namespace std;

void selection_sort(vector<int> vec){
    int small;
    int n=vec.size();
    for(int i=0;i<n-1;i++){
        small=i;
        for(int j=i+1;j<n;j++){
            if(vec[j]<vec[small]){
                small=j;
            }
        }
        swap(vec[i],vec[small]);
    }
    for(int i=0;i<n;i++){
        cout<<vec[i]<<" ";
    }
}
int main(){
    vector<int> vec;
    int size,c;
    cout<<"Size: ";
    cin>> size;
    for(int i=0;i<size;i++){
        cin>>c;
        vec.push_back(c);
    }
    selection_sort(vec);
}
