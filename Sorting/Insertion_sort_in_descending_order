#include<bits/stdc++.h>
using namespace std;

void insertion_sort(vector<int> vec){
    int n=vec.size();
    for(int i=1;i<n;i++){
        int key=vec[i];
        int prev=i-1;
        while(prev>=0 && vec[prev]<key){
            vec[prev+1]=vec[prev];
            prev--;
        }
        vec[prev+1]=key;
    }
    for(int i=0;i<n;i++){
        cout<<vec[i]<<" ";
    }
}
int main(){
    vector<int> vec;
    int size,c;
    cout<<"Size: ";
    cin>> size;
    for(int i=0;i<size;i++){
        cin>>c;
        vec.push_back(c);
    }
    insertion_sort(vec);
}
